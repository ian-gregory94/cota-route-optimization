# Candidates the waiting-model bracket promoted that the Model A screen buried
# (D16). splice|002|011|HIGFITN moves from rank 53 of 60 to rank 10 between the
# two ends, the largest move in the set; the truth lies between them, so it is
# carried into evaluation. Adding a candidate is the conservative error.
splice|002|011|HIGFITN
splice|005|006|NMURBEAN
